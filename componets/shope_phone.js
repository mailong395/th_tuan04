import { StyleSheet, Text, View } from "react-native";

export default function ShopPhone() {
  retrun(<View style={styles.main}></View>);
}

const styles = StyleSheet.create({
  main: {
    flex: 1,
  },
});
